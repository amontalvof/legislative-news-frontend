import { Box, Typography } from '@mui/material';

const Banner = () => {
    return (
        <Box className="banner">
            <Typography component="h1">Legislative News Aggregator</Typography>
        </Box>
    );
};

export default Banner;
